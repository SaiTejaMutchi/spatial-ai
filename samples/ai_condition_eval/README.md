# Development condition fixture

This is a **DEVELOPMENT CONDITION FIXTURE**. It is not real damage evidence. A synthetic stain is overlaid so the pipeline can demonstrate `surfaceId -> AI region -> registration -> geometry-owned quantity` when public ARKitScenes frames contain no supported condition.
