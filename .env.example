# Groq AI API key. Copy this file to .env and replace the placeholder.
# The key is never written into config files and never logged.
GROQ_API_KEY=your_groq_api_key_here

# The model id is pinned in config/ai_model_config.json. Do not set a
# substitute here; silent model fallback is forbidden.
